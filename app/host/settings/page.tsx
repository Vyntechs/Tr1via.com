import { ComingSoonPage } from "@/components/host/ComingSoonPage";

export default function HostSettingsPage() {
  return (
    <ComingSoonPage
      eyebrow="SETTINGS"
      title="Your account, your defaults."
      body="Profile, billing, account email, SMTP overrides, and integration keys live here. We'll wire it in as the platform matures."
    />
  );
}
