import { ComingSoonPage } from "@/components/host/ComingSoonPage";

export default function HostVenuesPage() {
  return (
    <ComingSoonPage
      eyebrow="VENUES"
      title="Every room you host in."
      body="Manage multiple venues from one host account. Each venue gets its own suggestions, default theme, and audience history."
    />
  );
}
