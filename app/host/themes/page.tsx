import { ComingSoonPage } from "@/components/host/ComingSoonPage";

export default function HostThemesPage() {
  return (
    <ComingSoonPage
      eyebrow="THEMES"
      title="Pick your room's personality."
      body="Color palettes you can swap per night. Each theme repaints the TV, host laptop, and player phones together. Customization moves here from the player surface — for now, every night runs the house theme."
    />
  );
}
